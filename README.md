This script is written to remove the bloatware which installs when users logs in to the Windows machine.
Script removes the apps from the exsisting users and prevent from installing on the new users.
